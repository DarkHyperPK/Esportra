# Supabase Migrations — DEPRECATED

**All database migrations live in the backend repo:**

```
esportra-backend/src/Esportra.Infrastructure/Migrations/Scripts/
```

Migrations are applied automatically on backend startup via **DbUp**.

Do **NOT** add new `.sql` migration files here. Add them to the backend repo instead.

/**
 * Pro-structure stage wizard — writes config.br (format, advancement, lobby formation).
 * Implementation lives in BRStageSetupWizard; this module is the canonical import path.
 */
export { default } from '@/components/organizer/br/BRStageSetupWizard';
export type { BrStageDtoInput } from '@/components/organizer/br/BRStageSetupWizard';

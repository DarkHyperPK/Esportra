ESPORTRA MIGRATION — REMAINING WORK
=====================================
Last updated: March 2026
Migration progress: 27/29 todos complete


─────────────────────────────────────────
1. BUILD VERIFICATION (DO THIS FIRST)
─────────────────────────────────────────
The CLI tool requires pwsh.exe on PATH to run commands.
After restarting the Copilot CLI, run these to confirm no compile errors:

  Backend (.NET):
    cd D:\esportra-backend
    dotnet build

  Frontend (TypeScript):
    cd D:\frag-and-book-main
    npm run build

Expected: zero errors. If errors appear, fix before proceeding.


─────────────────────────────────────────
2. FILE DELETION (F1 — partial)
─────────────────────────────────────────
App.tsx references to FaceitOrgTest have been removed, but the actual
files still exist on disk and need to be deleted:

  - D:\frag-and-book-main\src\pages\debug\FaceitOrgTest.tsx
  - D:\frag-and-book-main\supabase\functions\faceit-org-test\  (entire folder)

Commands:
  Remove-Item "D:\frag-and-book-main\src\pages\debug\FaceitOrgTest.tsx"
  Remove-Item -Recurse "D:\frag-and-book-main\supabase\functions\faceit-org-test"


─────────────────────────────────────────
3. EDGE FUNCTION DECOMMISSION (F3)
─────────────────────────────────────────
All Edge Functions have been replaced by .NET endpoints. Once the backend
is deployed to production and logs confirm traffic is flowing to .NET,
delete the remaining Supabase Edge Functions:

  supabase/functions/send-email/         → replaced by POST /api/emails
  supabase/functions/manage-users/       → replaced by POST /api/admin/users/{id}/action
  supabase/functions/invite-sponsor/     → replaced by POST /api/admin/invite-sponsor
  supabase/functions/scan-recent-matches/→ replaced by POST /api/matches/scan
  supabase/functions/process-match-result/→ replaced by POST /api/matches/{id}/process
  supabase/functions/record-metric/      → replaced by POST /api/sponsors/track

Steps:
  a) Deploy backend to production (Coolify)
  b) Monitor logs for 24-48h to confirm .NET endpoints are receiving traffic
  c) Delete each edge function folder from supabase/functions/
  d) Run: supabase functions deploy (to sync deletions to Supabase)
  e) Remove any edge function routes from supabase/config.toml if present


─────────────────────────────────────────
4. LOAD TEST (F5)
─────────────────────────────────────────
Target: 10,000 requests/sec sustained load.
Validates: Redis caching, rate limiter (100 req/60s per user), SignalR hubs.

Recommended tool: k6 (https://k6.io)

Sample k6 script targets to test:
  - GET  /api/tournaments       (cached, high read volume)
  - GET  /api/leaderboards/teams (cached aggregation)
  - POST /api/analytics/events  (rate-limit exempt, write path)
  - GET  /api/profiles/me       (auth + Redis cache warmup)

Rate limit config (appsettings.json):
  "RateLimit": {
    "MaxRequests": 100,
    "WindowSeconds": 60
  }
  Adjust MaxRequests upward for load test environment to avoid self-throttling.

Requires: running backend + Redis + PostgreSQL in staging/prod environment.


─────────────────────────────────────────
5. REMAINING SUPABASE USAGE (BY DESIGN)
─────────────────────────────────────────
These files intentionally still use Supabase — do NOT migrate them:

  Auth (permanent):
    src/hooks/useAuthState.ts       — supabase.auth session listener
    src/hooks/useAuthActions.ts     — supabase.auth sign in/up/out/OAuth
    src/contexts/AuthContext.tsx    — auth state + realtime suspension detection

  Storage (permanent):
    src/hooks/useTeamMutations.ts   — team logo/banner uploads to Supabase Storage

  Realtime subscriptions (migrate to SignalR when ChatHub is built):
    src/hooks/useMessaging.ts       — message INSERT events via supabase.channel
    src/hooks/useMapVetoMachine.ts  — notification inserts (fire-and-forget)

  Complex eligibility (keep on Supabase — multi-table JOIN):
    src/hooks/useReviews.ts         — canReview() checks venue_bookings +
                                      tournament_participants + team_members

  Admin utility (backend endpoint not yet implemented):
    src/hooks/useUserManagement.ts  — cleanupOrphanedProfiles() calls
                                      POST /api/admin/users/cleanup
                                      (stub — needs server-side auth.admin access)


─────────────────────────────────────────
WHAT WAS COMPLETED (for reference)
─────────────────────────────────────────
Batch A — Backend endpoints (8 items): AdminEndpoints, ReviewEndpoints,
  OrganizerEndpoints, PartnerEndpoints, StageEndpoints, LeaderboardEndpoints,
  MessagingEndpoints, VetoEndpoints, AnalyticsEndpoints

Batch B — Frontend quick wins (6 items): AdminContext, RoleContext,
  useAnalytics, useEmail, usePublicBracketData, useUserManagement

Batch C — Unblocked migrations (7 items): useTournamentWizard, useReviews,
  usePartnerApplication, useOrganizerStats, useAdminUsers, useSponsors,
  useMessaging

Batch D — Critical veto (1 item): useMapVetoMachine (3 mutations → .NET)

Batch E — Match processing (2 items): /api/matches/scan (Riot API),
  /api/matches/{id}/process (finalize_match_locked RPC)

Batch F — Polish (4/6 items): RateLimitMiddleware, Contracts cleanup,
  FaceitOrgTest App.tsx removal, dead import cleanup
